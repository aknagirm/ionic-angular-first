import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionicangular.first',
  appName: 'ionic-angular-first',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
